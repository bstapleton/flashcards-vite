export enum ErrorCode {
    NO_ELIGIBLE = 'nothing_eligible',
    MISMATCH = 'answer_mismatch',
    FORBIDDEN = 'unauthorized',
    NOT_FOUND = 'not_found',
    INVALID_CREDENTIALS = 'invalid_credentials',
}