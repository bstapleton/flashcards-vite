import React from 'react';

const Profile = () => {
    return (
        <main>
            <h1>Import</h1>
        </main>
    );
}
// TODO add a link to the import page as well as everything else
export default Profile;