import LoginForm from "../components/LoginForm/LoginForm.tsx";

const Login = () => {
    return (
        <main>
            <LoginForm title={null} />
        </main>
    );
}

export default Login;