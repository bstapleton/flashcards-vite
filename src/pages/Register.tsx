import RegisterForm from "../components/RegisterForm/RegisterForm.tsx";

const Register = () => {
    return (
        <main>
            <RegisterForm />
        </main>
    );
}

export default Register;